server:
  port: 8080

spring:
  application:
    name: SmartMedicine

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/smart_medicine
    username: root
    password: admin

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect

mqtt:
  broker-url: tcp://localhost:2003
  client-id: smartmedicine_backend_service
  username: "daovudat"
  password: "b23dccn127"
  timeout: 10
  keepalive: 60
  topics:
    sensor-data: sensor_data
    sensor-status: sensor_status
    device-control: device_control
    device-response: device_control_resp
    device-status-req: device_status_req
    device-status-resp: device_status_resp
