/**
 * Barrel export cho toàn bộ config của app.
 * Import từ "@/config" thay vì từng file riêng lẻ.
 *
 * @example
 *   import { HEADER_HEIGHT, NAV_ITEMS, SENSOR_META, CHART_CONFIG } from "@/config";
 */
export * from "./constants";
export * from "./navigation";
export * from "./sensorConfig";
